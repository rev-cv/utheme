[← Назад](../README.md)

---

# Настройка окружения (Windows)

В этой заметке описан процесс установки необходимых инструментов для работы над проектом.

## 1. WSL2 (Windows Subsystem for Linux)

Наличие WSL2 рекомендуется для корректной и быстрой работы Docker Desktop (режим WSL 2 backend).

**Установка.** Открыть PowerShell от имени администратора и выполнить команду:
```powershell
wsl --install
```
*После установки потребуется перезагрузка компьютера.*

Если уже стоял WSL1, убедиться, что дистрибутив переведен на версию 2:
```powershell
wsl --set-default-version 2
```

**Проверка успешности установки:**
```powershell
wsl --status
# Или проверка версии дистрибутива
wsl --list --verbose
```

## 2. Docker Desktop

Необходим для запуска локального сервера WordPress и базы данных.

**Установка:**
1. Скачать [Docker Desktop](https://docs.docker.com/desktop/setup/install/windows-install/). При установке разрешить использование WSL 2.
2. После установки зайти в `Settings -> General` и проверить галочку `Use the WSL 2 based engine`.

**Проверка успешности установки:**
```powershell
docker --version
# Docker version 28.x.x, build ...
```

## 3. Python & uv (менеджер пакетов)

Для работы скриптов автоматизации в папке `core/` используется связка Python + uv.

**Установка UV:**
```powershell
powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/install.ps1 | iex"
```

Установка Python не обязательна. UV сам загрузит нужную версию Python при первом запуске скрипта.

**Проверка успешности установки:**
```powershell
uv --version
```

## 4. Node.js & SASS

Необходим для сборки фронтенда.

**Установка Node.js:**
- Скачать [Node.js](https://nodejs.org/en/download/current).
- Установить.

**Проверка успешности установки:**
```powershell
node -v
npm -v
```

**Установка SASS:**
```powershell
npm install -g sass
```

**Проверка успешности установки:**
```powershell
sass --version
```

## 5. Git for Windows

Инструмент для клонирования репозитория и управления версиями кода.

**Установка Git:**
- Скачать и установить [Git for Windows](https://git-scm.com/).

**Проверка успешности установки:**
```powershell
git --version
```

## 💡 Возможные проблемы

Скрипты отключены: Если PowerShell выдает ошибку при запуске uv или sass, выполнить:
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

Переменные окружения (PATH): Если после установки команда не найдена («команда не является внутренней...»), попробовать перезагрузить терминал, чтобы пути обновились.