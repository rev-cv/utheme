services:
  wordpress:
    image: wordpress:latest
    container_name: "${CONTAINER_NAME:-wp_site}"
    ports:
      - "127.0.0.1:${HOST_PORT}:80"
    restart: unless-stopped
    environment:
      WORDPRESS_DB_HOST: wp_shared_db
      WORDPRESS_DB_USER: ${DB_USER}
      WORDPRESS_DB_PASSWORD: ${DB_PASSWORD}
      WORDPRESS_DB_NAME: ${DB_NAME}
    volumes:
      - ./utheme:/var/www/html/wp-content/themes/utheme
      - ./uploads:/var/www/html/wp-content/uploads
      - ./plugins:/var/www/html/wp-content/plugins
      - ./core/setup_site.sh:/var/www/html/setup_site.sh
      - ./core/uploads.ini:/usr/local/etc/php/conf.d/uploads.ini
      - ./core/.htaccess:/var/www/html/.htaccess
      - ./core/wp-config.php:/var/www/html/wp-config.php
    networks:
      - web_network
      - shared_db_network

    deploy:
      resources:
        limits:
          cpus: '${WP_CPU_LIMIT:-0.5}'
          memory: ${WP_MEM_LIMIT:-256m}

    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  sass:
    image: node:alpine
    working_dir: /src
    volumes:
      - ./utheme/src:/src
    profiles:
      - dev
    command: >
      sh -c "npm install -g sass@1.77.8 &&
      sass --watch --poll --style=compressed style.scss:style.css"

networks:
  web_network:
    external: true
  shared_db_network:
    external: true
